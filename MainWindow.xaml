<Window x:Class="KillerPDF.MainWindow" Icon="pack://application:,,,/Resources/kp-icon.ico"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="KillerPDF" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        Background="#333333">

    <Window.Resources>
        <!-- Killer Tools dark/green theme -->
        <SolidColorBrush x:Key="BgDark" Color="#333333"/>
        <SolidColorBrush x:Key="BgPanel" Color="#3a3a3a"/>
        <SolidColorBrush x:Key="BgHover" Color="#404040"/>
        <SolidColorBrush x:Key="BorderDim" Color="#2e2e2e"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#1ea54c"/>
        <SolidColorBrush x:Key="AccentGreenDim" Color="#0d2a18"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#e0e0e0"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#888888"/>
        <SolidColorBrush x:Key="DangerRed" Color="#ef4444"/>

        <!-- Toolbar button -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/SegoeMDL2Assets.ttf#Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                ToolTip="{TemplateBinding ToolTip}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ToolbarButtonAccent" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
        </Style>
        <Style x:Key="ToolbarButtonDanger" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Foreground" Value="{StaticResource DangerRed}"/>
        </Style>
        <Style x:Key="ToolbarToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontFamily" Value="pack://application:,,,/Resources/SegoeMDL2Assets.ttf#Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"
                                ToolTip="{TemplateBinding ToolTip}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChromeButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ChromeCloseButton" TargetType="Button" BasedOn="{StaticResource ChromeButton}">
            <Setter Property="Foreground" Value="{StaticResource DangerRed}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource DangerRed}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Thin green scrollbar -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="14"/>
            <Setter Property="MinWidth" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid>
                            <Border Background="#1a1a1a" CornerRadius="4" Margin="0"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True" Margin="2">
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Style>
                                            <Style TargetType="Thumb">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Thumb">
                                                            <Border x:Name="thumbBorder"
                                                                    Background="#551ea54c"
                                                                    CornerRadius="4"/>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="thumbBorder" Property="Background" Value="{StaticResource AccentGreen}"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Thumb.Style>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Height" Value="14"/>
                    <Setter Property="MinHeight" Value="14"/>
                    <Setter Property="Width" Value="Auto"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Dark context menu -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4" Padding="4">
                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="20,6,40,6"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="MenuBd" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}" CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{TemplateBinding InputGestureText}"
                                           Foreground="{StaticResource TextSecondary}" FontSize="11"
                                           VerticalAlignment="Center" Margin="24,0,0,0"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="MenuBd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource BorderDim}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="4,4"/>
        </Style>

        <!-- ======================================================== -->
        <!-- Dark ComboBox (zoom dropdown)                           -->
        <!-- ======================================================== -->

        <!-- Toggle button used inside the ComboBox template -->
        <Style x:Key="DarkComboToggle" TargetType="ToggleButton">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" BorderThickness="0"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBoxItem inside the dark popup -->
        <Style x:Key="DarkComboItem" TargetType="ComboBoxItem">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- The ComboBox itself -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <!-- Clickable area / border -->
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="18"/>
                                    </Grid.ColumnDefinitions>
                                    <!-- Selected item text (non-editable mode) -->
                                    <ContentPresenter x:Name="ContentSite"
                                                      Grid.Column="0"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      Margin="7,0,0,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      IsHitTestVisible="False"/>
                                    <!-- Editable text box (shown when IsEditable=True) -->
                                    <TextBox x:Name="PART_EditableTextBox"
                                             Grid.Column="0"
                                             Margin="5,0,0,0"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Foreground="{StaticResource TextPrimary}"
                                             FontFamily="Consolas"
                                             FontSize="11"
                                             VerticalAlignment="Center"
                                             IsReadOnly="{TemplateBinding IsReadOnly}"
                                             Visibility="Hidden"
                                             Focusable="True"
                                             SelectionBrush="{StaticResource AccentGreenDim}"
                                             SelectionTextBrush="{StaticResource AccentGreen}"
                                             CaretBrush="{StaticResource AccentGreen}"/>
                                    <!-- Chevron arrow -->
                                    <TextBlock Grid.Column="1"
                                               Text="&#xE70D;"
                                               FontFamily="pack://application:,,,/Resources/SegoeMDL2Assets.ttf#Segoe MDL2 Assets"
                                               FontSize="8"
                                               Foreground="{StaticResource TextSecondary}"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Center"
                                               IsHitTestVisible="False"/>
                                    <!-- Invisible toggle over the whole control -->
                                    <ToggleButton Grid.Column="0" Grid.ColumnSpan="2"
                                                  Style="{StaticResource DarkComboToggle}"
                                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay,
                                                              RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Grid>
                            </Border>
                            <!-- Dropdown popup -->
                            <Popup x:Name="PART_Popup"
                                   AllowsTransparency="True"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Focusable="False"
                                   PopupAnimation="Fade"
                                   Placement="Bottom">
                                <Border Background="{StaticResource BgPanel}"
                                        BorderBrush="{StaticResource AccentGreenDim}"
                                        BorderThickness="1"
                                        CornerRadius="3"
                                        Padding="2"
                                        MinWidth="{Binding ActualWidth,
                                                   RelativeSource={RelativeSource TemplatedParent}}"
                                        MaxHeight="220">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                            <Trigger Property="IsDropDownOpen" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Page list styles -->
        <Style x:Key="PageListBox" TargetType="ListBox">
            <Setter Property="Background" Value="#222222"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- General-purpose dark text button (use on non-icon buttons to bypass WPF default blue hover) -->
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BgPanel}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="28"/>
        </Grid.RowDefinitions>

        <!-- Title bar -->
        <Border Grid.Row="0" Background="#222222" BorderBrush="{StaticResource BorderDim}" BorderThickness="0,0,0,1"
                MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock Text="Killer" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="PDF" FontFamily="Segoe UI" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentGreen}"/>
                    <TextBlock Text="" x:Name="FileNameLabel" VerticalAlignment="Center" Margin="16,0,0,0"
                               Foreground="{StaticResource TextSecondary}" FontFamily="Consolas" FontSize="11"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="&#x2015;" Click="MinimizeBtn_Click" Style="{StaticResource ChromeButton}"/>
                    <Button Content="&#x25A1;" Click="MaximizeBtn_Click" Style="{StaticResource ChromeButton}"/>
                    <Button Content="&#x2715;" Click="CloseBtn_Click" Style="{StaticResource ChromeCloseButton}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#222222" BorderBrush="{StaticResource BorderDim}" BorderThickness="0,0,0,1" Padding="4,2">
            <Grid>
                <!-- File operations -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <!-- File: New / Open / Close / Save / Print -->
                    <Button Content="&#xE7C3;" Style="{StaticResource ToolbarButton}" Click="New_Click" ToolTip="新建空白文档 (Ctrl+N)"/>
                    <Button Content="&#xE8DA;" Style="{StaticResource ToolbarButton}" Click="Open_Click" ToolTip="打开 (Ctrl+O)"/>
                    <Button x:Name="CloseFileBtn" Content="&#xE711;" Style="{StaticResource ToolbarButton}" Click="CloseFile_Click" ToolTip="关闭文件 (Ctrl+W)" IsEnabled="False"/>
                    <Button x:Name="SaveAsBtn" Content="&#xE74E;" Style="{StaticResource ToolbarButtonAccent}" Click="SaveAs_Click" ToolTip="另存为 (Ctrl+Shift+S)"/>
                    <Button Content="&#xE8B9;" Style="{StaticResource ToolbarButton}" Click="SaveFlattened_Click" ToolTip="保存为扁平PDF（栅格化所有页面 — 完全不可编辑）"/>
                    <Button Content="&#xE749;" Style="{StaticResource ToolbarButton}" Click="Print_Click" ToolTip="打印 (Ctrl+P)"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <!-- Page ops: Merge / Extract -->
                    <Button Content="&#xE710;" Style="{StaticResource ToolbarButton}" Click="Merge_Click" ToolTip="合并PDF（将其他PDF合并到当前文档）"/>
                    <Button Content="&#xE8B1;" Style="{StaticResource ToolbarButton}" Click="Split_Click" ToolTip="提取页面（将选中页面保存为新PDF）"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <!-- Page ops: Delete / Move -->
                    <Button Content="&#xE8C6;" Style="{StaticResource ToolbarButton}" Click="Delete_Click" ToolTip="删除选中页面"/>
                    <Button Content="&#xE74A;" Style="{StaticResource ToolbarButton}" Click="MoveUp_Click" ToolTip="上移页面（重新排序）"/>
                    <Button Content="&#xE74B;" Style="{StaticResource ToolbarButton}" Click="MoveDown_Click" ToolTip="下移页面（重新排序）"/>
                </StackPanel>
                <!-- Edit tools + zoom -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="ToolSelectBtn" Content="&#xE8B3;" Style="{StaticResource ToolbarButton}" Click="ToolSelect_Click" ToolTip="选择工具 - 点击标注，拖动选择文本，双击编辑"/>
                    <Button x:Name="ToolTextBtn" Content="&#xE8D2;" Style="{StaticResource ToolbarButton}" Click="ToolText_Click" ToolTip="文字工具 - 点击添加文字"/>
                    <Button x:Name="ToolHighlightBtn" Content="&#xED56;" Style="{StaticResource ToolbarButton}" Click="ToolHighlight_Click" ToolTip="高亮工具 - 拖动高亮文本"/>
                    <Button x:Name="ToolDrawBtn" Content="&#xED63;" Style="{StaticResource ToolbarButton}" Click="ToolDraw_Click" ToolTip="绘制工具 - 手绘墨迹"/>
                    <Button x:Name="ToolCropBtn" Content="&#xE7A8;" Style="{StaticResource ToolbarButton}" Click="ToolCrop_Click" ToolTip="裁剪工具 - 拖动设置裁剪区域，然后应用到页面"/>
                    <Button x:Name="ToolImageBtn" Content="&#xEB9F;" Style="{StaticResource ToolbarButton}" Click="ToolImage_Click" ToolTip="图片工具 - 点击插入图片到页面"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <Button x:Name="ToolSignatureBtn" Content="&#xEE56;" Style="{StaticResource ToolbarButton}" Click="ToolSignature_Click" ToolTip="签名 - 创建并放置签名"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <Button Content="&#xE7A7;" Style="{StaticResource ToolbarButton}" Click="Undo_Click" ToolTip="撤销 (Ctrl+Z)"/>
                    <Button Content="&#xED62;" Style="{StaticResource ToolbarButtonDanger}" Click="ClearAnnotations_Click" ToolTip="清除所有标注"/>
                    <Rectangle Width="1" Fill="{StaticResource BorderDim}" Margin="4,6"/>
                    <ToggleButton x:Name="GridViewToggle"
                                  Content="&#xE8A9;"
                                  Style="{StaticResource ToolbarToggleButton}"
                                  IsChecked="True"
                                  IsEnabled="False"
                                  Click="GridViewToggle_Click"
                                  ToolTip="多页网格视图"
                                  Margin="0,0,4,0"/>
                    <ComboBox x:Name="ZoomBox" Width="96" Height="24" VerticalAlignment="Center"
                              Margin="0,0,4,0"
                              IsEditable="True" IsReadOnly="True"
                              Style="{StaticResource DarkComboBox}"
                              SelectionChanged="ZoomBox_SelectionChanged" ToolTip="缩放级别（Ctrl+滚动缩放）">
                        <ComboBoxItem Tag="fitwidth" IsSelected="True">适应宽度</ComboBoxItem>
                        <ComboBoxItem Tag="fitpage">适应页面</ComboBoxItem>
                        <ComboBoxItem Tag="0.5">50%</ComboBoxItem>
                        <ComboBoxItem Tag="0.75">75%</ComboBoxItem>
                        <ComboBoxItem Tag="1.0">100%</ComboBoxItem>
                        <ComboBoxItem Tag="1.25">125%</ComboBoxItem>
                        <ComboBoxItem Tag="1.5">150%</ComboBoxItem>
                        <ComboBoxItem Tag="2.0">200%</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content -->
        <Grid Grid.Row="2" x:Name="MainContentGrid">
            <Grid.ColumnDefinitions>
                <!-- SidebarCol never collapses to 0 — it shrinks to 24px (the toggle strip width). -->
                <ColumnDefinition x:Name="SidebarCol" Width="180" MinWidth="24" MaxWidth="260"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition x:Name="SearchPanelCol" Width="0" MinWidth="0" MaxWidth="320"/>
            </Grid.ColumnDefinitions>

            <!-- Sidebar column: inner grid splits content from permanent toggle strip -->
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>        <!-- page list content -->
                    <ColumnDefinition Width="24"/>       <!-- toggle strip — always visible -->
                </Grid.ColumnDefinitions>

                <!-- Page list content (hidden when collapsed) -->
                <Border x:Name="SidebarBorder" Grid.Column="0"
                        Background="#222222"
                        BorderBrush="{StaticResource BorderDim}" BorderThickness="0,0,0,0">
                    <DockPanel>
                        <!-- Sticky header — lives outside the ScrollViewer so it never scrolls away -->
                        <DockPanel DockPanel.Dock="Top" LastChildFill="False" Margin="12,8,8,4">
                            <ToggleButton x:Name="SidebarViewToggle" DockPanel.Dock="Left"
                                          Content="&#xE8A5;" ToolTip="切换页面/书签"
                                          Style="{StaticResource ToolbarToggleButton}"
                                          Width="24" Height="20" Padding="0" FontSize="12"
                                          VerticalAlignment="Center" Margin="0,0,6,0"
                                          Click="SidebarViewToggle_Click"/>
                            <TextBlock x:Name="SidebarHeaderLabel" Text="页面" DockPanel.Dock="Left"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontFamily="Segoe UI" FontSize="11" FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                            <TextBlock x:Name="PageTotalLabel" DockPanel.Dock="Right"
                                       Text="/ –" VerticalAlignment="Center"
                                       FontFamily="Consolas" FontSize="11"
                                       Foreground="{StaticResource TextSecondary}"
                                       Margin="2,0,0,0"/>
                            <TextBox x:Name="PageJumpBox" DockPanel.Dock="Right"
                                     Width="30" Height="20" VerticalAlignment="Center"
                                     TextAlignment="Center" VerticalContentAlignment="Center"
                                     FontFamily="Consolas" FontSize="11"
                                     Background="{StaticResource BgPanel}"
                                     Foreground="{StaticResource TextPrimary}"
                                     BorderBrush="{StaticResource BorderDim}" BorderThickness="1"
                                     CaretBrush="{StaticResource AccentGreen}"
                                     SelectionBrush="{StaticResource AccentGreenDim}"
                                     IsEnabled="False"
                                     FocusVisualStyle="{x:Null}"
                                     KeyDown="PageJumpBox_KeyDown"
                                     GotFocus="PageJumpBox_GotFocus"
                                     LostFocus="PageJumpBox_LostFocus"
                                     ToolTip="跳转到页面（输入后自动跳转）"
                                     Margin="0,0,2,0">
                                    <TextBox.Style>
                                        <Style TargetType="TextBox">
                                            <Setter Property="BorderBrush" Value="{StaticResource BorderDim}"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsFocused" Value="True">
                                                    <Setter Property="BorderBrush" Value="{StaticResource AccentGreen}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBox.Style>
                                </TextBox>
                        </DockPanel>
                        <!-- Scrollable page list -->
                        <ScrollViewer x:Name="SidebarScrollViewer"
                                      VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled"
                                      ScrollChanged="SidebarScrollViewer_ScrollChanged">
                            <Grid>
                                <ListBox x:Name="PageList" Style="{StaticResource PageListBox}"
                                         BorderThickness="0" SelectionMode="Extended"
                                         ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                         PreviewMouseWheel="PageList_PreviewMouseWheel"
                                         SelectionChanged="PageList_SelectionChanged"
                                         AllowDrop="True" Drop="PageList_Drop" DragOver="PageList_DragOver"
                                         PreviewMouseLeftButtonDown="PageList_PreviewMouseLeftButtonDown"
                                         PreviewMouseMove="PageList_PreviewMouseMove"
                                         MouseRightButtonUp="PageList_MouseRightButtonUp"/>
                                <TreeView x:Name="BookmarkTree" Visibility="Collapsed"
                                          Background="Transparent" BorderThickness="0"
                                          Foreground="{StaticResource TextPrimary}"
                                          FontFamily="Segoe UI" FontSize="12"
                                          SelectedItemChanged="BookmarkTree_SelectedItemChanged">
                                    <TreeView.ItemContainerStyle>
                                        <Style TargetType="TreeViewItem">
                                            <Setter Property="Background" Value="Transparent"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <Setter Property="Padding" Value="4,2"/>
                                            <Setter Property="Margin" Value="0"/>
                                            <Setter Property="Cursor" Value="Hand"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="TreeViewItem">
                                                        <StackPanel>
                                                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                                    BorderThickness="0"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter ContentSource="Header"/>
                                                            </Border>
                                                            <ItemsPresenter x:Name="ItemsHost"/>
                                                        </StackPanel>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHover}"/>
                                                            </Trigger>
                                                            <Trigger Property="IsSelected" Value="True">
                                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentGreenDim}"/>
                                                                <Setter Property="Foreground" Value="{StaticResource AccentGreen}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </TreeView.ItemContainerStyle>
                                </TreeView>
                            </Grid>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <!-- Toggle strip — always 24px wide, right edge of sidebar column -->
                <Border Grid.Column="1"
                        Background="#333333"
                        BorderBrush="{StaticResource BorderDim}"
                        BorderThickness="1,0,0,0">
                    <Button x:Name="SidebarToggleBtn"
                            Content="&#xE76B;" ToolTip="折叠侧边栏"
                            Style="{StaticResource ToolbarButton}"
                            Width="24" Padding="0" FontSize="10"
                            VerticalAlignment="Top" Margin="0,6,0,0"
                            Click="SidebarToggle_Click"/>
                </Border>
            </Grid>

            <!-- Preview area -->
            <Border Grid.Column="1" Background="{StaticResource BgDark}">
                <Grid>
                    <!-- Drop zone -->
                    <Border x:Name="DropZone" Background="Transparent"
                            AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver"
                            MouseLeftButtonDown="DropZone_Click">
                        <Border BorderBrush="{StaticResource BorderDim}" BorderThickness="2"
                                CornerRadius="8" Margin="40" Padding="40"
                                HorizontalAlignment="Center" VerticalAlignment="Center"
                                Background="Transparent" Style="{x:Null}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="将PDF拖放至此" FontFamily="Segoe UI" FontSize="20"
                                           Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center"/>
                                <TextBlock Text="或点击浏览" FontFamily="Segoe UI" FontSize="13"
                                           Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>
                    </Border>

                    <!-- PDF page with annotation overlay -->
                    <ScrollViewer x:Name="PagePreviewPanel" Visibility="Collapsed"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  Background="Transparent"
                                  PreviewMouseWheel="PagePreview_PreviewMouseWheel"
                                  SizeChanged="PagePreviewPanel_SizeChanged"
                                  PreviewMouseDown="PagePreviewPanel_PreviewMouseDown"
                                  PreviewMouseMove="PagePreviewPanel_PreviewMouseMove"
                                  PreviewMouseUp="PagePreviewPanel_PreviewMouseUp"
                                  AllowDrop="True" Drop="DropZone_Drop" DragOver="DropZone_DragOver">
                        <Border VerticalAlignment="Top" HorizontalAlignment="Center" Padding="12">
                            <Grid x:Name="PageContentGrid">
                                <Grid.LayoutTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Grid.LayoutTransform>
                                <WrapPanel x:Name="PageContentPanel" Orientation="Horizontal">
                                    <!-- Primary page (always present) -->
                                    <Border Background="White" Margin="0,0,12,12">
                                        <Grid>
                                            <Image x:Name="PageImage" RenderOptions.BitmapScalingMode="HighQuality" Stretch="None"/>
                                            <Canvas x:Name="AnnotationCanvas" Background="Transparent" ClipToBounds="True"
                                                    PreviewMouseLeftButtonDown="Canvas_MouseLeftButtonDown"
                                                    MouseMove="Canvas_MouseMove"
                                                    PreviewMouseLeftButtonUp="Canvas_MouseLeftButtonUp"/>
                                        </Grid>
                                    </Border>
                                    <!-- Additional pages rendered dynamically in code-behind -->
                                </WrapPanel>
                            </Grid>
                        </Border>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Search panel (right side) -->
            <Border x:Name="SearchPanelBorder" Grid.Column="2"
                    Background="#222222"
                    BorderBrush="{StaticResource BorderDim}"
                    BorderThickness="1,0,0,0"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Search header -->
                    <DockPanel Grid.Row="0" Margin="8,8,8,4" LastChildFill="False">
                        <TextBlock Text="搜索" DockPanel.Dock="Left"
                                   Foreground="{StaticResource TextSecondary}"
                                   FontFamily="Segoe UI" FontSize="11" FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <Button x:Name="SearchCloseBtn" DockPanel.Dock="Right"
                                Content="&#xE711;" ToolTip="关闭搜索 (Esc)"
                                Style="{StaticResource ToolbarButton}"
                                Width="24" Height="20" Padding="0" FontSize="10"
                                VerticalAlignment="Center"
                                Click="SearchCloseBtn_Click"/>
                    </DockPanel>

                    <!-- Search input -->
                    <StackPanel Grid.Row="0" Margin="8,28,8,4" Orientation="Horizontal">
                        <TextBox x:Name="SearchInputBox"
                                 Width="180" Height="24"
                                 FontFamily="Segoe UI" FontSize="12"
                                 Background="{StaticResource BgPanel}"
                                 Foreground="{StaticResource TextPrimary}"
                                 BorderBrush="{StaticResource BorderDim}" BorderThickness="1"
                                 CaretBrush="{StaticResource AccentGreen}"
                                 SelectionBrush="{StaticResource AccentGreenDim}"
                                 VerticalContentAlignment="Center"
                                 Padding="6,0"
                                 KeyDown="SearchInputBox_KeyDown"/>
                        <Button Content="&#xE721;" Width="32" Height="24" Margin="4,0,0,0"
                                FontFamily="pack://application:,,,/Resources/SegoeMDL2Assets.ttf#Segoe MDL2 Assets" FontSize="14"
                                Foreground="{StaticResource TextPrimary}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand"
                                ToolTip="搜索 (Enter)"
                                Click="SearchExecute_Click">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="3"
                                                        Padding="0">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource BgHover}"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <!-- Search results list -->
                    <ListBox x:Name="SearchResultList" Grid.Row="1"
                             Margin="8,4,8,4"
                             Background="{StaticResource BgPanel}"
                             BorderBrush="{StaticResource BorderDim}"
                             BorderThickness="1"
                             FontFamily="Segoe UI" FontSize="12"
                             Foreground="{StaticResource TextPrimary}"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             SelectionChanged="SearchResultList_SelectionChanged"/>

                    <!-- Search footer (total count) -->
                    <TextBlock x:Name="SearchTotalLabel" Grid.Row="2"
                               Margin="8,4,8,8"
                               Foreground="{StaticResource TextSecondary}"
                               FontFamily="Segoe UI" FontSize="11"
                               HorizontalAlignment="Right"
                               Text=""/>
                </Grid>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3" Background="#222222" BorderBrush="{StaticResource BorderDim}" BorderThickness="0,1,0,0">
            <Grid Margin="16,0">
                <TextBlock x:Name="StatusText" Text="就绪"
                           Foreground="{StaticResource TextSecondary}"
                           FontFamily="Segoe UI" FontSize="11" VerticalAlignment="Center"/>
                <!-- Portable mode indicator (hidden when installed) -->
                <StackPanel x:Name="PortableBadge" Orientation="Horizontal"
                            HorizontalAlignment="Center" VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="便携版" Foreground="#555555"
                               FontFamily="Consolas" FontSize="9"
                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button x:Name="InstallBtn" Content="安装 KillerPDF..."
                            Click="Install_Click"
                            FontFamily="Segoe UI" FontSize="10"
                            Foreground="{StaticResource AccentGreen}"
                            Background="Transparent"
                            BorderThickness="1" BorderBrush="#2a5a3a"
                            Padding="8,1" Cursor="Hand"
                            VerticalAlignment="Center">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="2" Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#1a3a28"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Click="ShortcutHelp_Click"
                            Cursor="Hand" VerticalAlignment="Center"
                            Margin="0,0,12,0" FocusVisualStyle="{x:Null}"
                            ToolTip="键盘快捷键 (Ctrl+?)">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock Text="?"
                                           Foreground="#555555"
                                           FontFamily="Consolas" FontSize="10"
                                           VerticalAlignment="Center"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <TextBlock x:Name="VersionLabel" Text="v1.4.1"
                               Foreground="#6a6a6a" FontFamily="Consolas"
                               FontSize="10" VerticalAlignment="Center" Margin="0,0,12,0"/>
                    <TextBlock FontSize="10" VerticalAlignment="Center"
                               FontFamily="Consolas">
                        <Run Text="&#x00A9; 2026 " Foreground="#6a6a6a"/>
                        <Hyperlink NavigateUri="https://thekiller.net"
                                   RequestNavigate="Hyperlink_RequestNavigate"
                                   Foreground="#6a6a6a" TextDecorations="None">
                            <Run Text="Steve the Killer"/>
                        </Hyperlink>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Keyboard shortcut overlay -->
        <Grid x:Name="ShortcutOverlay"
              Grid.RowSpan="4"
              Panel.ZIndex="10000"
              Visibility="Collapsed"
              Background="#BB000000"
              MouseLeftButtonDown="ShortcutOverlay_MouseLeftButtonDown">
            <Border Background="#1e1e1e"
                    BorderBrush="{StaticResource AccentGreen}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="28,16,28,22"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    MinWidth="480"
                    MaxWidth="640"
                    MouseLeftButtonDown="ShortcutOverlayCard_MouseLeftButtonDown">
                <StackPanel>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="键盘快捷键"
                                   FontFamily="Segoe UI" FontSize="15" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimary}"
                                   HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <Button Click="ShortcutOverlayClose_Click"
                                HorizontalAlignment="Right" VerticalAlignment="Center"
                                Cursor="Hand" FocusVisualStyle="{x:Null}"
                                Width="24" Height="24">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock Text="&#xE711;"
                                               FontFamily="pack://application:,,,/Resources/SegoeMDL2Assets.ttf#Segoe MDL2 Assets" FontSize="11"
                                               Foreground="{StaticResource TextSecondary}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}" Value="True">
                                                        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <!-- Column layout -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left column -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="文件" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+O" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="打开" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+W" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="关闭文件" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+N" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="新建空白文档" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+S" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="保存" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Shift+S" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="另存为" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+P" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="打印" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="导航" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="&#x2190; / &#x2192; or &#x2191; / &#x2193;" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="上一页 / 下一页" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Scroll" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="放大 / 缩小" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="中键拖动" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="平移视图" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                        </StackPanel>

                        <!-- Right column -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="编辑" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,0,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+Z" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="撤销" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Delete" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="删除选中标注" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Enter / Escape" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="确认 / 取消文字或裁剪" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="搜索与选择" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+F" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="查找 / 搜索" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Enter / Shift+Enter" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="下一个 / 上一个结果" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,4"><TextBlock Text="Ctrl+A" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="选择页面所有文字" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+C" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="复制选中文字" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>

                            <TextBlock Text="帮助" Foreground="{StaticResource AccentGreen}"
                                       FontFamily="Consolas" FontSize="10" FontWeight="SemiBold" Margin="0,12,0,4"/>
                            <DockPanel Margin="0,0,0,0"><TextBlock Text="Ctrl+?" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondary}" Width="120"/><TextBlock Text="显示此快捷键列表" FontSize="11" Foreground="{StaticResource TextPrimary}"/></DockPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Film grain overlay - main content area only -->
        <Border x:Name="GrainOverlay"
                Grid.Row="2"
                IsHitTestVisible="False"
                Opacity="0.15"
                Panel.ZIndex="9999">
            <Border.Background>
                <ImageBrush x:Name="GrainBrush"
                            TileMode="Tile"
                            ViewportUnits="Absolute"
                            Viewport="0,0,256,256"
                            Stretch="None"/>
            </Border.Background>
        </Border>
    </Grid>
</Window>
